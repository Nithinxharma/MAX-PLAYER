sed -i 's/Scaffold(/var showProviderSheet by remember { mutableStateOf(false) }\n    var selectedProviderId by remember { mutableStateOf<String?>(null) }\n\n    Scaffold(\n      floatingActionButton = {\n        ExtendedFloatingActionButton(\n          onClick = { showProviderSheet = true },\n          icon = { Icon(Icons.Default.Menu, contentDescription = null) },\n          text = { Text(providerRegistry.getEnabledProviders().find { it.id == selectedProviderId }?.name ?: "All Providers") },\n          containerColor = MaterialTheme.colorScheme.surfaceVariant,\n          contentColor = MaterialTheme.colorScheme.onSurfaceVariant\n        )\n      },/g' app/src/main/kotlin/xyz/mpv/rex/ui/browser/cinehub/CineHubScreen.kt
